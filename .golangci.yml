version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - gocyclo
    - prealloc
  exclusions:
    paths:
      - tests/e2e/assets
    rules:
      - linters: [gocyclo]
        path: _test\.go

formatters:
  exclusions:
    paths:
      - tests/e2e/assets
