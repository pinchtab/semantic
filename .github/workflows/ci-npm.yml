name: CI / npm

on:
  pull_request:
    branches: [main]
    paths:
      - 'npm/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  npm:
    uses: ./.github/workflows/reusable-npm.yml
    with:
      ref: ${{ github.sha }}
      version: '0.0.0-ci'
